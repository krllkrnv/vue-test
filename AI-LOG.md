# AI-log

Блок А писал сам. Сначала закрыл загрузку и ошибку, потом поиск по имени и разбиение на компоненты. Готовый код через ИИ попросил сверить, что ничего не сломалось, и помочь собрать текст ревью по `review/ReviewMe.vue`.

## Проверка кода блока А

**Промпт.** Посмотреть `App.vue`, `UserSearch.vue` и `UserCard.vue` и сказать, не сломал ли я загрузку, ошибку и фильтр.

**Что переписал руками и почему.** Замечания по сути не поменяли схему. Поправил только мелочи вроде имени переменной у поиска. Состояния, фильтр по имени и границу компонентов оставил как делал сам.

## `REVIEW.md`

**Промпт.** По `review/ReviewMe.vue` помочь собрать текст ревью: что ломается и как чинить.

**Что переписал руками и почему.** Сначала сам прочитал `review/ReviewMe.vue` и набросал список проблем. ИИ помог разложить это в связный текст. Формулировки потом правил сам.
